#!/bin/bash

me=$APPSH_INSTANCE_HOME/`basename $0`

echo "Starting" >> $me.log

env | grep TEST_PROPERTY > $me.env

for i in {1..10}
do
  echo "#$i: `date`" >> $me.log
  sleep 1
done

echo "Exiting" >> $me.log
